

spring:
  application:
    name: accessResourceInTest
  datasource:
    url: jdbc:postgresql://localhost:54321/testaccessresources
    username: members
    password:
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL10Dialect

  flyway:
    enabled: false
    user: members
    password:
    url: jdbc:postgresql://localhost:54321/secjavabackend
    locations: classpath:db/migration
    baseline-on-migrate: true

prenom: bernard



